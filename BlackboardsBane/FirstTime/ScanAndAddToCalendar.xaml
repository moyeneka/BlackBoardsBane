<Page x:Class="BlackboardsBane.FirstTime.ScanAndAddToCalendar"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:BlackboardsBane.FirstTime"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ScanAndAddToCalendar">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Name="infoLabel">
            BBB will now scan for any assignments due soon.
            This may take a while.
        </TextBlock>
        <Button Grid.Row="1" Name="startBtn" Content="Ok, start scan" Click="Button_Click"/>
        <ListBox Grid.Row="2" Name="assignmentListBox"></ListBox>
    </Grid>
</Page>
